default:
  suites:
    default:
      paths:
        - "%paths.base%/tests/behat"
      contexts:
        - Context\FeatureContext
  extensions:
    Behat\MinkExtension:
      base_url: http://localhost
      goutte: ~
      selenium2: ~
